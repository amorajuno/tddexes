import {Rover} from './rover';

const directions = ['N', 'E', 'S', 'W']
const currentDirection = []

export function turnL(direction) {
    if 
    
}
export function turnR(direction) {
    
}
export function move(x, y, direction)

export function percent(a, b) {
    const percentage = a*0.01;
    return percentage*b;
}